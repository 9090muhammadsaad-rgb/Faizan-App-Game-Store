//!/bin/faizanstore
// faizanstore programming languge
// Simple Game
Choose = .INPUT("Enter Number Start 1-9")endl;
check = random(1 , 9);

if choose == check:
   show {"You Are Win The Game"}
else:
     show {"Your are Loss The Game Because Number is ", $(check)}
done
